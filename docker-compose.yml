version: "3.1"
services:
  contai-pg:
    image: "bitnami/postgresql"
    environment:
      - POSTGRESQL_USERNAME=docker
      - POSTGRESQL_PASSWORD=docker
      - POSTGRESQL_DATABASE=contai-db
    ports:
      - "5433:5432"
